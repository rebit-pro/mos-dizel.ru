services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-/}
        VITE_APP_VERSION: ${VITE_APP_VERSION:-v1.7.0}
    ports:
      - "${APP_PORT:-8080}:80"
    restart: unless-stopped
